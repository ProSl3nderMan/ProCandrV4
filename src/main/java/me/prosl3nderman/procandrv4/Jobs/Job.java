package me.prosl3nderman.procandrv4.Jobs;

import org.bukkit.entity.Player;
import org.bukkit.event.player.PlayerInteractEvent;
import org.bukkit.event.player.PlayerRespawnEvent;

public class Job {

    public void setupJob() {
        //give player job items.
    }

    public void resetAndRemoveJob() {
        //resets the player's job and removes them
    }

    public void handleEvent(PlayerInteractEvent e) {
        //handles job event
    }

    public void onPlayerRespawn(PlayerRespawnEvent e) {
        //handles player respawn event
    }
}
